import React from 'react';
import { Navbar } from '../index';
import { Wrapper } from '../../ui';

const Header = () => {
  return (  
    <header>
      <Wrapper>
        <Navbar />
      </Wrapper>
    </header>
  );
}
 
export default Header;